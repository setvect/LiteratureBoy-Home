<?xml version="1.0" encoding="utf-8"?>
<dbScript xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<!-- 
		표준 SQL이 아닌 전용 문법의 DBMS는 본 파일을 복사하여 해당 DBMS에 맞추어 사용한다. 
		
		foreign key를 만들기 때문에  테이블의 생성 순서가 중요함.
		foreign key가 만들어 질 수 있도록 부모 테이블이 먼저 생성 되도록 함. 
	 -->
	
	<!-- create table and basic data -->

	<table name="TBAC_MAPPING">
		<creation>
			CREATE TABLE TBAC_MAPPING
			(GROUP_NO INTEGER NOT NULL,
			AGENT_ID VARCHAR(20) NOT NULL,
			PRIMARY KEY (GROUP_NO,AGENT_ID))		
		</creation>
		<defaultValue>
			<query>	ALTER TABLE TBAC_MAPPING ADD FOREIGN KEY (GROUP_NO)	REFERENCES TBAB_GROUP(GROUP_NO) </query>
			<query>	ALTER TABLE TBAC_MAPPING ADD FOREIGN KEY (AGENT_ID)	REFERENCES TBAA_AGENT(AGENT_ID)	</query>		
		</defaultValue>		
	</table>

</dbScript>   